<div id="editMajor" class="row whiteUI" >
<div class="container">
<h1>Majors</h1>
  <% @major.each do |major| %>
  <% if major.major %>
    <a href="<%= majors_path %>/<%= major.major %>"><%= major.major.titleize %></a><br/>
  <%end%>
  <% end %>
<br/>
 <% if user_admin? %>
 <a class="btn btn-default btn-lg" href=<%= new_major_path %>>New Major</a>
 <%end%>
  
</div>
</div>